
import React from "react" // eslint-disable-line no-unused-vars


// Preferred Pure Functional (Display) Components (Does not need lifecycle hooks or state)
const Home = () => (
	<div className="">
		<h1>Home Component</h1>
	</div>
)

export default Home
